# Ajustes na LP1: depoimentos com estrelas, checkout direto, texto sem "cara de IA"

**Contexto:** a LP1 (`/`) já está no ar e ficou muito boa. Esse prompt cobre uma leva de ajustes finos: redesign dos depoimentos (estrelas + avatar com iniciais), CTAs indo direto pro checkout em vez de passar pelo quiz, remover o campo de nome do checkout (o nome já é pego no onboarding), tirar um trecho de texto com "cara de IA" do onboarding, e um bug de âncora do menu que eu encontrei no caminho.

**IMPORTANTE: aplicar direto nos arquivos. NÃO SALVE COMO ASSET.**

---

## 1. Depoimentos com estrelas + avatar de iniciais (sem foto)

Isso aparece **duas vezes** no arquivo `artifacts/perguntas-de-conexao/src/App.tsx` — uma vez dentro do `LandingV2Quiz` (LP1) e uma vez dentro do `Home` (LP2, landing estática). Aplicar a mesma mudança nos dois lugares.

Achar (aparece 2x, idêntico):

```tsx
<section className="lp-social"><div className="lp-container"><p className="lp-eyebrow lp-eyebrow-center">o que dizem</p><h2 className="lp-h2">Casais que já usaram<br /><em>e não largam mais.</em></h2><div className="lp-social-grid"><blockquote className="lp-testimonial"><p>"Descobri coisas do meu marido em uma noite que 8 anos de casamento não tinham me mostrado."</p><footer>— Marina L. · Belo Horizonte</footer></blockquote><blockquote className="lp-testimonial"><p>"A gente fez a distância desaparecer por 40 minutos. Valeu mais que qualquer presente."</p><footer>— Rafael C. · Curitiba</footer></blockquote><blockquote className="lp-testimonial"><p>"Virou nosso ritual de sexta à noite. Sério."</p><footer>— Julia M. · São Paulo</footer></blockquote></div><p className="lp-tiny-note">Depoimentos de casais em fase de teste.</p></div></section>
```

Substituir as DUAS ocorrências por (repare que os nomes viram "Casal" — dois primeiros nomes — pra combinar com o formato do print de referência):

```tsx
<section className="lp-social"><div className="lp-container"><p className="lp-eyebrow lp-eyebrow-center">o que dizem</p><h2 className="lp-h2">Casais que já usaram<br /><em>e não largam mais.</em></h2><div className="lp-social-grid">{[
  { initials: 'ML', name: 'Marina & Léo', location: 'Belo Horizonte', text: 'Descobri coisas do meu marido em uma noite que 8 anos de casamento não tinham me mostrado.' },
  { initials: 'RC', name: 'Rafael & Carla', location: 'Curitiba', text: 'A gente fez a distância desaparecer por 40 minutos. Valeu mais que qualquer presente.' },
  { initials: 'JM', name: 'Julia & Miguel', location: 'São Paulo', text: 'Virou nosso ritual de sexta à noite. Sério.' },
].map((item) => (
  <blockquote key={item.initials} className="lp-testimonial">
    <div className="lp-testimonial-stars" aria-label="5 de 5 estrelas">{'★★★★★'}</div>
    <p>"{item.text}"</p>
    <footer>
      <span className="lp-testimonial-avatar">{item.initials}</span>
      <span className="lp-testimonial-who"><strong>{item.name}</strong><span>{item.location}</span></span>
    </footer>
  </blockquote>
))}</div><p className="lp-tiny-note">Depoimentos de casais em fase de teste.</p></div></section>
```

### CSS — no final de `artifacts/perguntas-de-conexao/src/index.css`

```css
.lp-testimonial-stars {
  color: #ffc59e;
  font-size: .85rem;
  letter-spacing: .15em;
  margin-bottom: .6rem;
}
.lp-testimonial footer {
  display: flex;
  align-items: center;
  gap: .65rem;
  margin-top: .9rem;
}
.lp-testimonial-avatar {
  flex-shrink: 0;
  width: 38px;
  height: 38px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, rgba(196,172,255,.28), rgba(255,157,196,.22));
  border: 1px solid rgba(196,172,255,.35);
  color: #f9f8fb;
  font-size: .8rem;
  font-weight: 700;
  letter-spacing: .02em;
}
.lp-testimonial-who { display: flex; flex-direction: column; line-height: 1.3; }
.lp-testimonial-who strong { font-size: .85rem; font-weight: 600; color: #f9f8fb; }
.lp-testimonial-who span { font-size: .76rem; color: rgba(255,255,255,.5); }
```

**Nota:** os nomes seguem sendo iniciais/primeiros nomes fictícios ilustrativos (nenhuma foto real, nenhuma avaliação verificada) — o aviso "Depoimentos de casais em fase de teste" continua embaixo. Isso é consistente com o que já tínhamos combinado antes.

---

## 2. CTAs indo direto pro checkout (só na LP1)

Hoje o botão principal do hero da LP1 rola a página até o quiz, em vez de abrir o pagamento direto. Vamos inverter: o CTA principal do hero abre o checkout na hora, e quem quiser "testar antes" clica num link secundário que leva ao quiz.

Achar, dentro de `LandingV2Quiz`, o botão do hero:

```tsx
<button onClick={() => document.getElementById('lp2-quiz')?.scrollIntoView({ behavior: 'smooth' })} className="lp-cta-primary lp-cta-big lp2-hero-cta" data-testid="button-hero-cta-v2">Quero reacender a conexão <ArrowRight size={18} /></button>
```

Substituir por (agora abre o checkout direto, e ganha um link secundário embaixo pro quiz):

```tsx
<button onClick={onBuy} className="lp-cta-primary lp-cta-big lp2-hero-cta" data-testid="button-hero-cta-v2">Quero reacender a conexão <ArrowRight size={18} /></button>
<button type="button" onClick={() => document.getElementById('lp2-quiz')?.scrollIntoView({ behavior: 'smooth' })} className="lp-cta-secondary-link" data-testid="link-hero-quiz">Prefere ver um exemplo antes? Responda 3 perguntas rápidas →</button>
```

### CSS — no final de `index.css`

```css
.lp-cta-secondary-link {
  display: block;
  margin-top: .9rem;
  background: none;
  border: none;
  color: rgba(255,255,255,.55);
  font-size: .85rem;
  text-decoration: underline;
  text-underline-offset: 3px;
  cursor: pointer;
  padding: 0;
}
.lp-cta-secondary-link:hover { color: #c4acff; }
```

---

## 3. Texto do CTA no resultado do quiz — tirar "Ver o preço"

Achar, dentro de `LandingQuiz`:

```tsx
<p className="lp-quiz-cta-text"><strong>Essas são 3 de 31.</strong> Destrave as outras + os outros 14 baralhos:</p>
<button onClick={onFinish} className="lp-cta-primary lp-cta-big" data-testid="button-quiz-cta">Ver o preço <ArrowRight size={18} /></button>
```

Substituir por:

```tsx
<p className="lp-quiz-cta-text"><strong>Essas são 3 de 31.</strong> Destrave as outras + os outros 14 baralhos:</p>
<button onClick={onFinish} className="lp-cta-primary lp-cta-big" data-testid="button-quiz-cta">Quero aprofundar meu relacionamento <ArrowRight size={18} /></button>
```

---

## 4. Tirar o campo de nome do checkout (o nome já é pego no onboarding)

Achar a função `checkout` dentro de `Home`:

```tsx
const checkout = async () => {
    if (!buyerNameInput.trim()) return;
    setCheckoutState('sending');
    try {
      const response = await fetch(apiUrl('/api/checkout/create'), {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          packageId: selectedPackage,
          buyerName: buyerNameInput.trim(),
        }),
      });
```

Substituir por (usa um nome genérico só pra satisfazer a API — o nome de verdade é capturado depois, no onboarding):

```tsx
const checkout = async () => {
    setCheckoutState('sending');
    try {
      const response = await fetch(apiUrl('/api/checkout/create'), {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          packageId: selectedPackage,
          buyerName: 'Cliente Perguntas de Conexão',
        }),
      });
```

Achar a linha que declara o estado (pode remover, já que não é mais usado):

```tsx
const [buyerNameInput, setBuyerNameInput] = useState('');
```

Pode deixar essa linha OU remover — se remover, tem que remover também toda referência a `buyerNameInput`/`setBuyerNameInput` mais abaixo (ver próximo passo). Se preferir a via mais segura e rápida, **deixe a linha existindo** (não quebra nada, só fica sem uso) e mexa só no JSX do modal, no próximo passo.

Achar, no modal de checkout, o trecho com o input de nome:

```tsx
<><p className="section-kicker">acesso vitalício</p><h2>Seu baralho começa<br /><em>com uma pergunta.</em></h2><p>Você vai para a tela segura da Abacate Pay para pagar via Pix. O acesso só libera depois da confirmação.</p><input type="text" placeholder="Seu nome" value={buyerNameInput} onChange={event => setBuyerNameInput(event.target.value)} className="checkout-name-input" data-testid="input-checkout-name" />{checkoutState === 'error' && <p className="checkout-error">Não deu para iniciar ou confirmar o pagamento agora. Tente novamente em instantes.</p>}<button onClick={() => void checkout()} disabled={checkoutState === 'sending' || !buyerNameInput.trim()} className="button button-primary button-full" data-testid="button-confirm-checkout">{checkoutState === 'sending' ? 'Abrindo pagamento…' : 'Continuar para pagamento'} <ArrowRight size={16} /></button></>
```

Substituir por (sem o input de nome, botão já habilitado):

```tsx
<><p className="section-kicker">acesso vitalício</p><h2>Seu baralho começa<br /><em>com uma pergunta.</em></h2><p>Você vai para a tela segura da Abacate Pay para pagar via Pix. O acesso só libera depois da confirmação.</p>{checkoutState === 'error' && <p className="checkout-error">Não deu para iniciar ou confirmar o pagamento agora. Tente novamente em instantes.</p>}<button onClick={() => void checkout()} disabled={checkoutState === 'sending'} className="button button-primary button-full" data-testid="button-confirm-checkout">{checkoutState === 'sending' ? 'Abrindo pagamento…' : 'Continuar para pagamento'} <ArrowRight size={16} /></button></>
```

---

## 5. Onboarding — tirar o trecho com "cara de IA"

Achar, em `artifacts/perguntas-de-conexao/src/pages/Onboarding.tsx`:

```tsx
<p>Oi, eu sou a equipe por trás das Perguntas de Conexão.</p>
<p>Há algum tempo, percebemos uma coisa: a gente pode amar alguém e ainda assim não saber mais o que perguntar.</p>
<p>As melhores conversas não acontecem por acaso. Elas começam com uma pergunta que ninguém pensou em fazer.</p>
<p>Foi por isso que criamos este espaço — para ajudar você a conhecer essa pessoa de novo.</p>
```

Substituir por um texto mais direto e pessoal (sem o "oi, eu sou a equipe"):

```tsx
<p>Percebi uma coisa com o tempo: dá pra amar alguém e, mesmo assim, não saber mais o que perguntar.</p>
<p>As melhores conversas não acontecem por acaso. Elas começam com uma pergunta que ninguém pensou em fazer.</p>
<p>Foi por isso que criei este espaço — pra te ajudar a conhecer essa pessoa de novo.</p>
```

---

## 6. Menu do site — link de sugestões + correção de âncora quebrada na LP1

Achar, no componente `Shell` (mesmo `App.tsx`):

```tsx
<nav className={`main-nav ${menuOpen ? 'nav-open' : ''}`}>
        <Link href="/app" data-testid="link-experience">Experiência</Link>
        <a href="#como-funciona" data-testid="link-how-it-works">Como funciona</a>
       <a href="#lp-precos" data-testid="link-packages">Pacotes</a>
      </nav>
```

Substituir por (adiciona o link de sugestões por email):

```tsx
<nav className={`main-nav ${menuOpen ? 'nav-open' : ''}`}>
        <Link href="/app" data-testid="link-experience">Experiência</Link>
        <a href="#como-funciona" data-testid="link-how-it-works">Como funciona</a>
       <a href="#lp-precos" data-testid="link-packages">Pacotes</a>
        <a href="mailto:quinzinhooliveiraa@gmail.com?subject=Sugestão%20-%20Perguntas%20de%20Conexão" data-testid="link-feedback">Sugestões</a>
      </nav>
```

**Nota:** usei seu email pessoal como destino provisório do "Sugestões" — troque pra um email tipo `contato@perguntasdeconexao.com.br` assim que o domínio e um email profissional estiverem no ar.

Agora o bug de âncora: o menu aponta pros ids `#como-funciona` e `#lp-precos`, mas na LP1 (`LandingV2Quiz`) esses ids têm o prefixo `lp2-` (`lp2-como-funciona`, `lp2-precos`), então clicar em "Como funciona" ou "Pacotes" enquanto está na LP1 não rola pra lugar nenhum.

Achar, dentro de `LandingV2Quiz`:

```tsx
<section className="lp-how" id="lp2-como-funciona">
```

Substituir por:

```tsx
<section className="lp-how" id="como-funciona">
```

Achar:

```tsx
<section className="lp-price" id="lp2-precos">
```

Substituir por:

```tsx
<section className="lp-price" id="lp-precos">
```

(Não precisa mudar o `id="lp2-quiz"` nem o `id="lp2-pacotes"` nem `id="lp2-faq"` — esses não são referenciados pelo menu, só pelos scrolls internos da própria página.)

---

## Verificação

1. Abrir `/` → depoimentos aparecem com 5 estrelas, avatar com iniciais (sem foto) e nome tipo "Marina & Léo · Belo Horizonte".
2. Botão principal do hero ("Quero reacender a conexão") abre o checkout direto, sem passar pelo quiz.
3. Abaixo dele, aparece um link discreto "Prefere ver um exemplo antes?" que rola até o quiz.
4. Terminar o quiz → botão agora diz "Quero aprofundar meu relacionamento" (não mais "Ver o preço") e abre o checkout.
5. Abrir o checkout (qualquer CTA) → não aparece mais campo de nome, só o botão "Continuar para pagamento", já habilitado.
6. No menu do topo, clicar em "Como funciona" e "Pacotes" estando na `/` → rola corretamente até as seções (antes não rolava).
7. Clicar em "Sugestões" no menu → abre o app de email com destinatário preenchido.
8. Fazer o onboarding (`/onboarding`) → a tela da "nota" não tem mais "Oi, eu sou a equipe por trás...".