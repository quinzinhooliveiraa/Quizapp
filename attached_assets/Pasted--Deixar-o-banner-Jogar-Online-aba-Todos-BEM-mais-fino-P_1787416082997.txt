# Deixar o banner "Jogar Online" (aba Todos) BEM mais fino

**Problema:** o banner no topo do "Todos" ficou grande demais — tapa o título "Escolha um objetivo pra este momento" que vem embaixo. Precisa ser slim, uma linha só.

**IMPORTANTE: aplicar direto nos arquivos. NÃO SALVE COMO ASSET.**

## 1. Trocar o conteúdo do banner — `artifacts/perguntas-de-conexao/src/App.tsx`

Achar o `<button ... className="play-banner-cta">...</button>` que colocamos na aba Todos. Substituir o conteúdo INTERNO por (mais enxuto, sem kicker, sem subtítulo grande):

```tsx
{isOwner && (
  <button
    type="button"
    onClick={() => setLocation('/play')}
    className="play-banner-cta play-banner-slim"
    data-testid="button-play-online-top"
  >
    <div className="play-banner-icon">
      <Wifi size={16} />
    </div>
    <span className="play-banner-inline">
      <strong>Jogar online</strong>
      <small>abre uma sala e manda o código</small>
    </span>
    <ArrowRight size={16} className="play-banner-arrow" />
  </button>
)}
```

## 2. CSS — substituir/adicionar em `artifacts/perguntas-de-conexao/src/index.css`

Achar `.play-banner-cta` e apagar TODAS as regras antigas dele (incluindo `.play-banner-glow`, `.play-banner-text`, `.play-banner-kicker`). Substituir por:

```css
/* Banner slim topo da aba Todos */
.play-banner-cta {
  display: flex;
  align-items: center;
  gap: 0.7rem;
  width: 100%;
  padding: 0.65rem 1rem;
  margin: 0 0 1.2rem;
  background: linear-gradient(90deg, rgba(120, 90, 200, 0.14), rgba(196, 172, 255, 0.05));
  border: 1px solid rgba(196, 172, 255, 0.22);
  border-radius: 0.7rem;
  color: #f6f5fa;
  cursor: pointer;
  text-align: left;
  transition: all 0.15s ease;
}
.play-banner-cta:hover {
  border-color: rgba(196, 172, 255, 0.45);
  background: linear-gradient(90deg, rgba(120, 90, 200, 0.2), rgba(196, 172, 255, 0.08));
}
.play-banner-icon {
  width: 30px; height: 30px;
  border-radius: 50%;
  background: rgba(196, 172, 255, 0.2);
  color: #c4acff;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
}
.play-banner-inline {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 0.05rem;
  min-width: 0;
}
.play-banner-inline strong {
  font-size: 0.88rem;
  font-weight: 500;
  color: #f6f5fa;
  line-height: 1.2;
}
.play-banner-inline small {
  font-size: 0.72rem;
  color: rgba(255, 255, 255, 0.55);
  line-height: 1.2;
}
.play-banner-arrow {
  color: rgba(196, 172, 255, 0.7);
  flex-shrink: 0;
  transition: transform 0.15s ease;
}
.play-banner-cta:hover .play-banner-arrow { transform: translateX(3px); }

@media (min-width: 640px) {
  /* Desktop: pode ficar numa linha só, colocando o subtitle ao lado */
  .play-banner-inline {
    flex-direction: row;
    align-items: baseline;
    gap: 0.6rem;
  }
  .play-banner-inline small::before {
    content: "· ";
    color: rgba(255, 255, 255, 0.3);
  }
}
```

## Verificação
- Aba "Todos" no mobile: banner agora tem **~48px de altura** (bem fininho), icon + "Jogar online" + "abre uma sala e manda o código" + seta. Não tapa mais o título "Escolha um objetivo pra este momento" embaixo
- No desktop: fica em UMA linha só (título e subtítulo lado a lado com "·")
- O card grande do Meu Espaço fica IGUAL (não muda) — só o banner do "Todos" que fica mais discreto