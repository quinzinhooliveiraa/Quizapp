# Aplicar EXATAMENTE este patch no checkout (sem inventar nada)

Você é um agente de código. Sua ÚNICA tarefa é aplicar o diff abaixo **exatamente
como está**, sem redesenhar, sem reescrever nada além do que o diff mostra, sem
criar telas novas, sem "melhorar" nada. São só 2 arquivos:
`artifacts/perguntas-de-conexao/src/App.tsx` e
`artifacts/perguntas-de-conexao/src/index.css`.

Passos:
1. Aplique o patch unificado abaixo. Se tiver a ferramenta de shell, salve o
   conteúdo entre as marcas `===PATCH INÍCIO===` e `===PATCH FIM===` num arquivo
   `checkout-fix.patch` na raiz do repositório e rode `git apply checkout-fix.patch`
   (se falhar, `git apply --3way checkout-fix.patch`). Se não tiver shell, faça as
   MESMAS trocas manualmente nos dois arquivos, seguindo linha a linha o diff
   (linhas com `-` saem, linhas com `+` entram; as demais são contexto e ficam).
2. NÃO altere mais nenhum arquivo. NÃO mexa em backend, rotas, env, Stripe,
   AbacatePay, webhook, nem na lógica de pagamento. É só apresentação (JSX/CSS).
3. Rode `pnpm --filter @workspace/perguntas-de-conexao typecheck` e o build. Os
   dois têm que passar (esse patch já foi testado e passa limpo).
4. Faça commit e deploy como de costume.

Resumo do que o patch faz (pra você conferir, não pra reinterpretar): deixa o
painel do método de pagamento COMPACTO dentro do acordeão (remove a tela grande
"Falta só o Pix/cartão · e a conversa começa" que estava sendo espremida lá
dentro e quebrando o layout); mostra o Pix como QR + código copia-e-cola + botão
"Copiar" vinho com texto branco; mostra o cartão como o Stripe Payment Element
inline; empilha os métodos em linhas (uma embaixo da outra, não em 2 colunas); e
mantém tudo numa tela só (as transições que iam pras telas separadas
`native-payment`/`card-payment` passam a ficar na tela do formulário `email`).

===PATCH INÍCIO===
From 6a78b8f3c2b1ed876612e3addf0aaffe3b0a1265 Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Tue, 1 Sep 2026 23:56:16 +0000
Subject: [PATCH] Checkout: acordeao inline compacto (Pix QR + copia-e-cola, cartao inline)

---
 artifacts/perguntas-de-conexao/src/App.tsx   | 130 +++++--------------
 artifacts/perguntas-de-conexao/src/index.css |  96 +++++++++++++-
 2 files changed, 128 insertions(+), 98 deletions(-)

diff --git a/artifacts/perguntas-de-conexao/src/App.tsx b/artifacts/perguntas-de-conexao/src/App.tsx
index 6fe65ad..ae6744f 100644
--- a/artifacts/perguntas-de-conexao/src/App.tsx
+++ b/artifacts/perguntas-de-conexao/src/App.tsx
@@ -2295,7 +2295,7 @@ function useCheckout({
         setCardError(
           "O pagamento foi enviado, mas a confirmação ainda não chegou. Aguarde um pouco e tente verificar novamente.",
         );
-        setCheckoutState("card-payment");
+        setCheckoutState("email");
         return;
       }
 
@@ -2404,7 +2404,7 @@ function useCheckout({
       setNativeCheckout(pix);
       safeSetItem("conexao-pending-pix", JSON.stringify(pix));
       setSelectedPaymentMethod("pix");
-      if (!inline) setCheckoutState("native-payment");
+      if (!inline) setCheckoutState("email");
     } catch {
       if (inline) {
         setPaymentError(
@@ -2423,7 +2423,7 @@ function useCheckout({
     if (cardCheckout) {
       setCardError("");
       setSelectedPaymentMethod("card");
-      if (!inline) setCheckoutState("card-payment");
+      if (!inline) setCheckoutState("email");
       return;
     }
 
@@ -2479,7 +2479,7 @@ function useCheckout({
       safeSetItem("conexao-pending-at", String(checkoutStartedAt));
       safeSetItem("conexao-pending-card", JSON.stringify(card));
       setCardCheckout(card);
-      if (!inline) setCheckoutState("card-payment");
+      if (!inline) setCheckoutState("email");
     } catch {
       const message =
         "Não foi possível abrir o pagamento com cartão. Tente novamente ou escolha o Pix.";
@@ -2501,7 +2501,7 @@ function useCheckout({
     }
     setCardError("");
     setSelectedPaymentMethod("pix");
-    if (nativeCheckout) setCheckoutState("native-payment");
+    if (nativeCheckout) setCheckoutState("email");
   };
 
   const handleCardPaymentSubmitted = () => {
@@ -2892,22 +2892,7 @@ function CheckoutModal({ checkout }: { checkout: CheckoutController }) {
   };
 
   const pixPaymentContent = nativeCheckout ? (
-    <>
-      <p className="section-kicker">seu baralho está reservado</p>
-      <p className="checkout-native-status" role="status" aria-live="polite">
-        Pix pronto · aguardando confirmação
-      </p>
-      <h2>
-        Falta só o Pix
-        <br />
-        <em>e a conversa começa.</em>
-      </h2>
-      <p className="checkout-native-recap">
-        459 perguntas · 15 baralhos · acesso vitalício, sem mensalidade
-      </p>
-      <div className="checkout-native-price">
-        <strong>R$ 47,90, uma vez só</strong>
-      </div>
+    <div className="checkout-pix-inline">
       <div className="checkout-qr-wrap">
         <img
           src={
@@ -2917,59 +2902,33 @@ function CheckoutModal({ checkout }: { checkout: CheckoutController }) {
           }
           alt="QR Code do Pix"
         />
-        <p>Abra o app do seu banco e escaneie o código.</p>
       </div>
-      <button
-        className="checkout-copy-button"
-        type="button"
-        onClick={async () => {
-          const copied = await copyPixCode(nativeCheckout.brCode);
-          if (copied) {
-            setCopiedCode(true);
-            window.setTimeout(() => setCopiedCode(false), 2200);
-          } else {
-            setCopiedCode(false);
-          }
-        }}
-        data-testid="button-copy-pix"
-      >
-        {copiedCode ? <Check size={16} /> : <Copy size={16} />}
-        {copiedCode ? "Código copiado!" : "Copiar"}
-      </button>
-      <p className="checkout-native-next">
-        Assim que cair, seu acesso abre sozinho.
+      <p className="checkout-pix-hint">
+        Escaneie o QR ou copie o código.
+        <br />
+        <strong>Assim que cair, seu acesso abre sozinho.</strong>
       </p>
-      <div className="checkout-guarantee">
-        <Check size={17} />
-        <span>
-          <strong>Você tem 7 dias de garantia.</strong>
-          <br />
-          Se não fizer sentido pra vocês, devolvemos seu dinheiro.
-        </span>
+      <div className="checkout-pix-copybox">
+        <code>{nativeCheckout.brCode}</code>
+        <button
+          className="checkout-copy-button"
+          type="button"
+          onClick={async () => {
+            const copied = await copyPixCode(nativeCheckout.brCode);
+            if (copied) {
+              setCopiedCode(true);
+              window.setTimeout(() => setCopiedCode(false), 2200);
+            } else {
+              setCopiedCode(false);
+            }
+          }}
+          data-testid="button-copy-pix"
+        >
+          {copiedCode ? <Check size={16} /> : <Copy size={16} />}
+          {copiedCode ? "Copiado!" : "Copiar"}
+        </button>
       </div>
-      {checkoutReviews.length > 0 && (
-        <div className="checkout-reviews">
-          <p className="checkout-reviews-title">quem já abriu essa conversa</p>
-          {checkoutReviews.map((review) => (
-            <blockquote key={review.id}>
-              <div className="checkout-review-meta">
-                <div
-                  className="checkout-review-stars"
-                  aria-label={`${review.rating} de 5 estrelas`}
-                >
-                  {"★".repeat(Math.min(5, Math.max(0, review.rating)))}
-                </div>
-                <span className="checkout-review-rating">
-                  {review.rating}/5
-                </span>
-              </div>
-              <p>“{review.message}”</p>
-              <cite>{review.displayName!.trim()}</cite>
-            </blockquote>
-          ))}
-        </div>
-      )}
-    </>
+    </div>
   ) : paymentCreating === "pix" ? (
     <p className="checkout-payment-preview" role="status" aria-live="polite">
       Abrindo o QR Code aqui…
@@ -2980,27 +2939,12 @@ function CheckoutModal({ checkout }: { checkout: CheckoutController }) {
     </p>
   ) : (
     <p className="checkout-payment-preview">
-      Preencha seus dados para abrir o QR Code aqui.
+      Toque em “Garantir meu deck” que o QR aparece aqui.
     </p>
   );
 
   const cardPaymentContent = cardCheckout ? (
-    <>
-      <p className="section-kicker">seu baralho está reservado</p>
-      <p className="checkout-native-status" role="status">
-        Cartão · pagamento seguro
-      </p>
-      <h2>
-        Falta só o cartão
-        <br />
-        <em>e a conversa começa.</em>
-      </h2>
-      <p className="checkout-native-recap">
-        459 perguntas · 15 baralhos · acesso vitalício, sem mensalidade
-      </p>
-      <div className="checkout-native-price">
-        <strong>R$ 47,90, uma vez só</strong>
-      </div>
+    <div className="checkout-card-inline">
       <Elements
         stripe={stripePromise}
         options={{
@@ -3038,15 +2982,7 @@ function CheckoutModal({ checkout }: { checkout: CheckoutController }) {
           showSubmitButton={checkoutState !== "email"}
         />
       </Elements>
-      <div className="checkout-guarantee">
-        <Check size={17} />
-        <span>
-          <strong>Você tem 7 dias de garantia.</strong>
-          <br />
-          Se não fizer sentido pra vocês, devolvemos seu dinheiro.
-        </span>
-      </div>
-    </>
+    </div>
   ) : paymentCreating === "card" ? (
     <p className="checkout-payment-preview" role="status" aria-live="polite">
       Abrindo o pagamento com cartão aqui…
diff --git a/artifacts/perguntas-de-conexao/src/index.css b/artifacts/perguntas-de-conexao/src/index.css
index 99d4686..d386306 100644
--- a/artifacts/perguntas-de-conexao/src/index.css
+++ b/artifacts/perguntas-de-conexao/src/index.css
@@ -20969,7 +20969,7 @@ blockquote em {
 
 .checkout-payment-tabs {
   gap: 0.65rem;
-  grid-template-columns: repeat(2, minmax(0, 1fr));
+  grid-template-columns: 1fr;
 }
 
 .checkout-payment-item {
@@ -21011,6 +21011,100 @@ blockquote em {
   font-weight: 700;
 }
 
+/* --- Pix compacto dentro do acordeão --- */
+.checkout-pix-inline {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 0.85rem;
+}
+
+.checkout-pix-inline .checkout-qr-wrap {
+  margin: 0;
+}
+
+.checkout-pix-inline .checkout-qr-wrap img {
+  width: 156px;
+  height: 156px;
+}
+
+.checkout-pix-hint {
+  margin: 0;
+  text-align: center;
+  color: #6f6580;
+  font-size: 0.8rem;
+  line-height: 1.45;
+}
+
+.checkout-pix-hint strong {
+  color: #2fa36b;
+}
+
+.checkout-pix-copybox {
+  display: flex;
+  align-items: center;
+  gap: 0.5rem;
+  width: 100%;
+  padding: 0.6rem 0.7rem;
+  border: 1px dashed #cdbfd6;
+  border-radius: 11px;
+  background: #faf7fc;
+}
+
+.checkout-pix-copybox code {
+  flex: 1;
+  min-width: 0;
+  overflow: hidden;
+  color: #6f6580;
+  font-family: ui-monospace, "SFMono-Regular", Menlo, monospace;
+  font-size: 0.72rem;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+/* botão copiar sempre vinho + texto branco (legível) */
+.checkout-payment-panel .checkout-copy-button,
+.checkout-pix-copybox .checkout-copy-button {
+  display: inline-flex;
+  flex: none;
+  align-items: center;
+  gap: 0.35rem;
+  padding: 0.55rem 0.9rem;
+  border: none;
+  border-radius: 9px;
+  background: #7a2e46;
+  color: #fff;
+  font-family: inherit;
+  font-size: 0.78rem;
+  font-weight: 700;
+  cursor: pointer;
+}
+
+.checkout-payment-panel .checkout-copy-button:hover,
+.checkout-pix-copybox .checkout-copy-button:hover {
+  background: #5b2034;
+  color: #fff;
+}
+
+.checkout-card-inline {
+  display: block;
+}
+
+/* garante linha horizontal (ícone · texto · seleção), não empilhado */
+.checkout-payment-tabs button {
+  flex-direction: row;
+  justify-content: flex-start;
+  text-align: left;
+}
+
+.checkout-payment-tabs button .payment-tab-copy {
+  align-items: flex-start;
+}
+
+.checkout-payment-tabs button .payment-tab-check {
+  margin-left: auto;
+}
+
 .checkout-payment-panel .checkout-native-status {
   margin-top: 0;
 }
===PATCH FIM===