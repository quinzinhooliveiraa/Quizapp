auto-install-peers=false
strict-peer-dependencies=false
# The Replit runtime provides pnpm; let the runtime use it locally while
# packageManager pins the version used by Railway.
manage-package-manager-versions=false
